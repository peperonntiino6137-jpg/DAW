# task.md — 修正・要望の受付リスト

ここに修正してほしい箇所・要望を自由に書き込んでください（箇条書きでOK、書式は問いません）。
Claude が定期的に読み取り、上から順にサブエージェントへ割り振って処理します。

## 書き方の例

- [ ] オブジェクトのトラック割り当てを選べるようにしてほしい
- [ ] TOP VIEW の円をもう少し大きく

処理中は `[~]`、完了は `[x]` に変えて、下の「処理ログ」に結果を追記します。
急ぎのものは行頭に `!` を付けてください（最優先で処理します）。

---

## 受付中

- [~] オブジェクトとトラックのリンク（割り当て）を編集できるようにする（2026-08-20 ユーザー依頼、着手中）

---

## 処理ログ

- 2026-08-20 Claude: task.md を設置。運用開始。
- 2026-08-20 Claude: [x] 回帰テストランナーが Windows (Git Bash) で動かない問題を修正（python3 スタブ回避・file:// URL のパス形式・cp932 対策。test/run.sh、738/738 pass）。
- 2026-08-20 Claude: [x] 「曲の再生ができない」を修正。原因は file://（DAW.exe 起動）で AudioWorklet の blob: URL が AbortError になり、play() がリミッター準備で落ちて無反応になること。blob: → data: URL に変更（limiter.js / record.js / dyneq.js）し、失敗時もリミッター素通しで再生を続ける安全網を audio.js に追加。exe 再ビルド済み。
